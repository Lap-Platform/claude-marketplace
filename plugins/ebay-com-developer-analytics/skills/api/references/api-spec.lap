@lap v0.3
# Machine-readable API spec. Each @endpoint block is one API call.
@api Analytics API
@base https://api.ebay.com{basePath}
@version v1_beta.0.1
@auth OAuth2
@endpoints 2
@toc rate_limit(1), user_rate_limit(1)

@group rate_limit
@endpoint GET /rate_limit/
@optional {api_context: str, api_name: str}
@returns(200) {rateLimits: [map]}
@returns(204)
@errors {500}

@endgroup

@group user_rate_limit
@endpoint GET /user_rate_limit/
@optional {api_context: str, api_name: str}
@returns(200) {rateLimits: [map]}
@returns(204)
@errors {500}

@endgroup

@end
