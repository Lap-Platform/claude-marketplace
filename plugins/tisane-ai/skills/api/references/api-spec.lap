@lap v0.3
# Machine-readable API spec. Each @endpoint block is one API call.
@api Tisane API Documentation
@base https://api.tisane.ai
@auth ApiKey Ocp-Apim-Subscription-Key in header
@endpoints 8
@toc parse(1), languages(1), helper(1), compare(1), similarity(1), detectLanguage(1), transform(1), lm(1)

@group parse
@endpoint POST /parse
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200) {text: str, entities_summary: [map]}
@returns(200) {text: str, abuse: [map], sentiment_expressions: [map]}
@returns(200) {text: str, topics: [str]}
@returns(200) {text: str, topics: [str]}
@returns(200) {text: str, topics: [str], sentiment_expressions: [map], entities_summary: [map]}
@returns(200) {text: str, topics: [str], sentence_list: [map]}
@returns(200) {text: str, topics: [any], abuse: [map], sentence_list: [map]}
@returns(200) {text: str, topics: [str], abuse: [map], sentence_list: [map]}

@endgroup

@group languages
@endpoint GET /languages
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200) {id: str, name: str, englishName: str, nativeEncoding: str, preferredFont: str, latin: bool, rightToLeft: bool}

@endgroup

@group helper
@endpoint POST /helper/extract_text
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200)

@endgroup

@group compare
@endpoint POST /compare/entities
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200) {result: str, differences: [str]}

@endgroup

@group similarity
@endpoint POST /similarity
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200)

@endgroup

@group detectLanguage
@endpoint POST /detectLanguage
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200) {languages: [map]}
@returns(200) {languages: [map]}
@returns(200) {languages: [map]}

@endgroup

@group transform
@endpoint POST /transform
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200)
@returns(200)
@returns(200)
@returns(200)
@returns(200)

@endgroup

@group lm
@endpoint GET /lm/inflections
@required {language: str, lexeme: str, family: str}
@optional {Ocp-Apim-Subscription-Key: str}
@returns(200) {features: [map], text: str}
@returns(200) {features: [map], isLemma: bool, text: str}

@endgroup

@end
