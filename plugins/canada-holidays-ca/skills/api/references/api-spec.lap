@lap v0.3
# Machine-readable API spec. Each @endpoint block is one API call.
@api Canada Holidays API
@base https://canada-holidays.ca
@version 1.8.0
@endpoints 6
@toc api(6)

@endpoint GET /api/v1
@returns(200) {message: str, _links: map{self: map{href: str}, holidays: map{href: str}, provinces: map{href: str}, spec: map{href: str}}}

@endpoint GET /api/v1/holidays
@optional {year: int=2023, federal: str(1/0/true/false), optional: str(1/0/true/false)=false}
@returns(200) {holidays: [map]}

@endpoint GET /api/v1/provinces
@optional {year: int=2023, optional: str(1/0/true/false)=false}
@returns(200) {provinces: [map]}

@endpoint GET /api/v1/provinces/{provinceId}
@optional {year: int=2023, optional: str(1/0/true/false)=false}
@returns(200) {province: map{id: str, nextHoliday: map{id: int, date: str(date), nameEn: str, nameFr: str, federal: int(binary), observedDate: str(date), provinces: [map], optional: int(binary)}, provinces: [map], nameFr: str, nameEn: str, sourceLink: str(uri), sourceEn: str, optional: int(binary)}}
@errors {400}

@endpoint GET /api/v1/holidays/{holidayId}
@optional {year: int=2023, optional: str(1/0/true/false)=false}
@returns(200) {holiday: map{id: int, date: str(date), nameEn: str, nameFr: str, federal: int(binary), observedDate: str(date), provinces: [map], optional: int(binary)}}
@errors {400}

@endpoint GET /api/v1/spec
@returns(200)

@end
