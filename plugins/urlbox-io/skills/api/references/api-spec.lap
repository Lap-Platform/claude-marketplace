@lap v0.3
# Machine-readable API spec. Each @endpoint block is one API call.
@api Urlbox API
@base https://api.urlbox.io
@version v1
@auth Bearer bearer
@endpoints 1
@toc render(1)

@endpoint POST /v1/render/sync
@optional {format: str(png/jpg/pdf/svg/mp4/webp/webm/html), url: str, html: str, width: int, height: int, block_ads: bool, hide_cookie_banners: bool, click_accept: bool, gpu: bool, retina: bool, thumb_width: int, thumb_height: int, full_page: bool, selector: str, delay: str, wait_until: str(requestsfinished/mostrequestsfinished/loaded/domloaded), metadata: bool, wait_for: str, wait_to_leave: str}
@returns(200) {renderUrl: str(uri), size: int(int64)}
@errors {307, 400, 401, 500}

@end
